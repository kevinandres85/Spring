server:
  port: 8083

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/kevinandres85/Spring.git
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}
          search-paths: config-data
  security:
    user:
      name: root
      password: s3cr3t


